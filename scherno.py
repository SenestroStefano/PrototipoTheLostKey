"""from screeninfo import get_monitors
import global_var as GLOB

for monitor in get_monitors():
    width = monitor.width
    height = monitor.height
 
    print(str(width/GLOB.screen_width) + 'x' + str(height/GLOB.screen_height))

    GLOB.MULT = width/GLOB.screen_width"""